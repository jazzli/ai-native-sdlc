---
import NoteRoute from '../../components/NoteRoute.astro';
import { noteParams } from '../../lib/note-routes';

export const getStaticPaths = () => noteParams('questions');
const { note, canonical } = Astro.props;
---

<NoteRoute note={note} canonical={canonical} />
